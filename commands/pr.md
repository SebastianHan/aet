---
description: PR management for creating, updating and tracking platform pull requests. Use natural language to create PRs, update PRs, or list PRs.
disable-model-invocation: true
---

Automatically detect the language of user input and respond in the same language.

Invoke the pr skill with the provided natural language arguments, then execute exactly as the skill presents.